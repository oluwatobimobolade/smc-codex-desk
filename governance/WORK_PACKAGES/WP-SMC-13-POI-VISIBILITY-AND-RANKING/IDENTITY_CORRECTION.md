# Historical work-package identity correction

The directory and append-only validation record named
`WP-SMC-13-POI-VISIBILITY-AND-RANKING` were created after governance had
already reserved `WP-SMC-13` for the independent analyst-selected development
cohort. They are retained so historical evidence is not rewritten or deleted,
but the POI/runtime repair is not the canonical WP-SMC-13 programme.

The corrected identity is `WP-SMC-14-POI-CONTRACT-AND-NATIVE-MARKUP-REPAIR`.
The canonical WP-SMC-13 remains the human truth gate described in
`governance/NEXT_ACTIONS.yaml`: independent selection of 12–15 cases and one
expert markup pass before threshold tuning.
