config_id: PERCEPTION_DETECTOR_CONFIG_V2
status: split_contract_ready_code_migration_pending
authority: perception_detector_parameters_only
source_monolith: specs/PERCEPTION_ONTOLOGY_V2.yaml

ontology_version: "2.0.0"

geometry_and_tolerances:
  equal_level_tolerance_bps: 15.0
  equal_level_min_touches: 2

structural_scales:
  swing_scales:
    local: 1
    internal: 3
    external: 5

fair_value_gaps:
  minimum_gap_bps: 5.0
  displacement_factor: 1.05

structure_breaks:
  break_confirmation:
    wick_cross_creates_candidate: true
    body_close_confirms: true
    displacement_required: false
  structure_break_min_bps: 4.0
  displacement_body_factor: 1.3

order_blocks:
  ob_lookback: 8
  ob_min_body_factor: 0.75

sweeps:
  liquidity_sweep_lookback: 80

detector_limits:
  lookback_bars: 250
  atr_lookback: 14

forbidden_in_this_file:
  - risk_reward_floor
  - structural_stop_margin_bps
  - stop_buffer_atr_mult
  - require_fresh_poi
  - allowed_poi_kinds
  - strategy_target_policy
  - capital_risk
